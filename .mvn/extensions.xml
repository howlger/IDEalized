<extensions>
    <extension>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-build</artifactId>

        <!-- Tycho version see: .mvn/maven.config -->
        <version>${tycho-version}</version>

    </extension>
</extensions>